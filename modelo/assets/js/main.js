// capturar o evento de submit do formulario
const form = document.querSelector('#formulario');

form.addEventListener('submit', function(evento) {
    e.preventDefault();
    console.log('Evento previnido.');

});