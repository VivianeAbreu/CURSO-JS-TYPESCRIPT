// String, number, undefined, null, boolean

const nome = 'Viviane'; //String
const nome1 = "Viviane"; //String
const nome2 = `Viviane`; //String

const num1 = 10; // number
const num2 = 10.52; // number

let nomeAluno; // Undefined = nao aponta pra local nenhum na memoria
const sobrenomeAluno = null; // Nulo = nao aponta pra local nenhum na memoria
const aprovado =true // boolean = tipo de dados e so tem duas possibilidades de valores que e true ou false (valor lógico)

console.log(typeof nome, nome); // typeof tipo da operacao