
// Não podemos criar constantes com palavras reservadas
// constantes precisam ter nomes sgnificativos
// Não pode comecar o nome de uma constantes com um numero
// Não podem conter espaços ou traços
// variaveis com nomes compostos utilizamos CameIcase
// Case-sensintive letra maiuscula e minuscula fazem diferença
// Não uilize VAR sempre utilize CONST
// Nao posso declarar uma constante sem declarar ela
// + - * /
// dentro de aspas String = Text| fora de aspas e Number = numero

const nome = 'João';
console.log(nome);

const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
console.log(primeiroNumero);

console.log(typeof primeiroNumero);
